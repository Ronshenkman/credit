{
    "name": "credit-viz",
    "version": "1.0.0",
    "description": "Credit card visualization with backend persistence",
    "scripts": {
        "install-all": "npm install --prefix webapp && npm install --prefix server",
        "build": "npm run build --prefix webapp",
        "start": "node server/index.js"
    }
}